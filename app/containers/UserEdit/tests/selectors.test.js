// import { fromJS } from 'immutable';
// import { makeSelectUserEditDomain } from '../selectors';

// const selector = makeSelectUserEditDomain();

describe('makeSelectUserEditDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
