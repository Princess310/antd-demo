// import { fromJS } from 'immutable';
// import { makeSelectResetMobileDomain } from '../selectors';

// const selector = makeSelectResetMobileDomain();

describe('makeSelectResetMobileDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
