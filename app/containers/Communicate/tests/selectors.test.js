// import { fromJS } from 'immutable';
// import { makeSelectCommunicateDomain } from '../selectors';

// const selector = makeSelectCommunicateDomain();

describe('makeSelectCommunicateDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
