// import { fromJS } from 'immutable';
// import { makeSelectUserBlackListDomain } from '../selectors';

// const selector = makeSelectUserBlackListDomain();

describe('makeSelectUserBlackListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
