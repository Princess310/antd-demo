// import { fromJS } from 'immutable';
// import { makeSelectUserSettingPageDomain } from '../selectors';

// const selector = makeSelectUserSettingPageDomain();

describe('makeSelectUserSettingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
