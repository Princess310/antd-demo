// import { fromJS } from 'immutable';
// import { makeSelectFeedBackPageDomain } from '../selectors';

// const selector = makeSelectFeedBackPageDomain();

describe('makeSelectFeedBackPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
