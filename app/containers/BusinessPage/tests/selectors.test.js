// import { fromJS } from 'immutable';
// import { makeSelectBusinessPageDomain } from '../selectors';

// const selector = makeSelectBusinessPageDomain();

describe('makeSelectBusinessPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
