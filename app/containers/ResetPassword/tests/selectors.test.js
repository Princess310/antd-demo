// import { fromJS } from 'immutable';
// import { makeSelectResetPasswordDomain } from '../selectors';

// const selector = makeSelectResetPasswordDomain();

describe('makeSelectResetPasswordDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
